// Shared design tokens for the public landing page.
// Brand palette: white surface, near-black ink, single red accent.

/** Geist Mono utility — used for eyebrow labels, tags, and numeric callouts. */
export const MONO = "font-[family-name:var(--font-geist-mono)]";

/** Signature red accent (Radix Red 9 — premium, crisp on white). */
export const RED = "#E5484D";

/** Standard horizontal container. */
export const CONTAINER = "mx-auto w-full max-w-6xl px-6";
